# Инструкции по развертыванию

## 📤 Выгрузка на GitHub

### Вариант 1: Создание нового репозитория

1. Перейдите на GitHub и создайте новый репозиторий:
   - Название: `casino-games-collection` (или любое другое)
   - Не инициализируйте с README, .gitignore или лицензией

2. Подключите ваш локальный репозиторий к GitHub:

```bash
cd "/Users/simonshmelev/Desktop/мой проект"
git remote add origin https://github.com/Orcagoldenwhale/ВАШЕ_НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git push -u origin main
```

### Вариант 2: Использование существующего репозитория

Если вы хотите использовать существующий репозиторий `video-timestamp-app`:

```bash
cd "/Users/simonshmelev/Desktop/мой проект"
git remote add origin https://github.com/Orcagoldenwhale/video-timestamp-app.git
git push -u origin main
```

**⚠️ Внимание:** Это перезапишет существующий репозиторий!

### Если нужна авторизация

При использовании HTTPS может потребоваться Personal Access Token:
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Создайте новый token с правами `repo`
3. Используйте его как пароль при push

## 🚀 Развертывание на Render.com

### Шаг 1: Подключение репозитория

1. Зайдите на [render.com](https://render.com) и войдите через GitHub
2. Нажмите "New +" → "Static Site"
3. Подключите ваш GitHub репозиторий
4. Выберите репозиторий с проектом

### Шаг 2: Настройка проекта

**Имя сервиса:** `casino-games` (или любое другое)

**Настройки:**
- **Build Command:** `npm install`
- **Publish Directory:** `.` (точка - корневая папка)
- **Start Command:** оставьте пустым (для Static Site не нужен)

**Или используйте автоматическую конфигурацию через `render.yaml`:**

Render автоматически обнаружит файл `render.yaml` и применит настройки.

### Шаг 3: Переменные окружения (опционально)

Если нужны переменные окружения, добавьте их в настройках:
- Нажмите "Environment" в панели управления
- Добавьте нужные переменные

### Шаг 4: Деплой

1. Нажмите "Create Static Site"
2. Дождитесь завершения деплоя
3. Ваш сайт будет доступен по адресу: `https://casino-games.onrender.com` (или ваш кастомный домен)

## 🔄 Обновление проекта

После внесения изменений в код:

```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

Render автоматически обнаружит изменения и запустит новый деплой.

## 📝 Примечания

- Render бесплатно предоставляет статические сайты
- Автоматический деплой при каждом push в main ветку
- Можно настроить кастомный домен в настройках проекта
- Для статических сайтов нет необходимости в переменной `PORT`, но в package.json она используется для совместимости

## 🔐 Предоставление доступа другим

### На GitHub:

1. Откройте репозиторий на GitHub
2. Settings → Collaborators → Add people
3. Добавьте email или username пользователя
4. Выберите права доступа (Read, Write, Admin)

### На Render:

1. В настройках проекта → Collaborators
2. Добавьте email пользователя
3. Выберите уровень доступа

## 🆘 Проблемы и решения

**Проблема:** Render не может найти index.html
- **Решение:** Убедитесь, что Publish Directory указан как `.` (корневая папка)

**Проблема:** Сайт не загружается
- **Решение:** Проверьте Build Logs в панели Render для ошибок

**Проблема:** Не работает push на GitHub
- **Решение:** Проверьте права доступа к репозиторию и наличие Personal Access Token

