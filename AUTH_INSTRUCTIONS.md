# 🔐 Инструкция по авторизации для GitHub

## Вариант 1: Personal Access Token (Рекомендуется)

### Шаг 1: Создайте токен на GitHub

1. Перейдите на https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Задайте название токена: `Casino Games Project`
4. Выберите срок действия (рекомендую 90 дней или "No expiration")
5. **Отметьте галочку** на **`repo`** (это даст полный доступ к репозиториям)
6. Прокрутите вниз и нажмите **"Generate token"**
7. **ВАЖНО:** Скопируйте токен сразу! Он больше не будет показан.

### Шаг 2: Используйте токен для push

Выполните в терминале:

```bash
cd "/Users/simonshmelev/Desktop/мой проект"
git push -u origin main
```

Когда запросит:
- **Username:** `Orcagoldenwhale` (ваш GitHub username)
- **Password:** вставьте скопированный токен (НЕ ваш обычный пароль!)

## Вариант 2: SSH ключи (Для постоянной работы)

### Шаг 1: Проверьте наличие SSH ключей

```bash
ls -al ~/.ssh
```

### Шаг 2: Создайте SSH ключ (если нет)

```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

Нажмите Enter для всех вопросов (используйте стандартные настройки).

### Шаг 3: Добавьте ключ в ssh-agent

```bash
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
```

### Шаг 4: Скопируйте публичный ключ

```bash
cat ~/.ssh/id_ed25519.pub
```

Скопируйте весь вывод.

### Шаг 5: Добавьте ключ на GitHub

1. Перейдите на https://github.com/settings/keys
2. Нажмите **"New SSH key"**
3. Title: `MacBook` (или любое название)
4. Key: вставьте скопированный ключ
5. Нажмите **"Add SSH key"**

### Шаг 6: Измените URL репозитория на SSH

```bash
cd "/Users/simonshmelev/Desktop/мой проект"
git remote set-url origin git@github.com:Orcagoldenwhale/video-timestamp-app.git
git push -u origin main
```

## ✅ Проверка

После успешного push, откройте:
https://github.com/Orcagoldenwhale/video-timestamp-app

Вы должны увидеть все ваши файлы!

